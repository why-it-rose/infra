# HTTP 전용 (인증서 발급 전 초기 상태)
# 인증서 발급 후에는 entrypoint.sh가 default-https.conf.template으로 교체함
server {
    listen 80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
